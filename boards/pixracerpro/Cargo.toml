[package]
name = "pixracerpro"
version = "0.1.0"
edition = "2024"

[features]
default = []
scope-timing-pins = ["veloxity_core/scope-timing-pins"]
sensor-poll-diagnostics = []

[dependencies]
stm_32 = { workspace = true }
veloxity_core = { workspace = true }
veloxity_mavlink = { workspace = true }
heapless = { version = "0.8.0", default-features = false }

panic-halt = "1.0.0"

[lints]
workspace = true

[[bin]]
name = "veloxity"
test = false
bench = false
doc = false
